name: Blender 3.6.23 aarch64 cross-build
on: [workflow_dispatch]

env:
  BLENDER_BRANCH: blender-v3.6-release
  BLENDER_VERSION: 3.6.23

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Install host tools
        run: |
          sudo apt update
          sudo apt install -y git cmake ninja-build build-essential wget curl xz-utils \
          python3 python3-pip pkg-config autoconf automake bison libtool yasm tcl meson \
          gcc-aarch64-linux-gnu g++-aarch64-linux-gnu patchelf

      - name: Blender source
        run: |
          git clone --depth 1 -b "${BLENDER_BRANCH}" https://github.com/blender/blender.git blender
          cd blender
          git submodule update --init --recursive --depth 1
          git clone --depth 1 https://projects.blender.org/blender/blender-addons.git scripts/addons

      - name: Pre-download broken deps
        run: |
          cd blender
          mkdir -p ../build_linux/deps/packages
          
          wget -q -O ../build_linux/deps/packages/mesa-21.1.5.tar.xz https://archive.mesa3d.org/older-versions/21.x/mesa-21.1.5.tar.xz
          wget -q -O ../build_linux/deps/packages/glu-9.0.1.tar.xz https://archive.mesa3d.org/glu/glu-9.0.1.tar.xz
          wget -q -0 ../build_linux/deps/packages/gmp-6.2.1.tar.xz https://ftp.gnu.org/gnu/gmp/gmp-6.2.1.tar.xz

      - name: Build dependencies for aarch64
        run: |
          cd blender
          export BLENDER_DOWNLOAD_TIMEOUT=600
          make deps -j$(nproc)

      - name: Configure
        run: |
          cd blender
          mkdir -p build
          cd build
          cmake .. -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_SYSTEM_NAME=Linux \
            -DCMAKE_SYSTEM_PROCESSOR=aarch64 \
            -DCMAKE_C_COMPILER=aarch64-linux-gnu-gcc \
            -DCMAKE_CXX_COMPILER=aarch64-linux-gnu-g++ \
            -DWITH_LIBS_PRECOMPILED=ON \
            -DWITH_CYCLES_CUDA=OFF -DWITH_CYCLES_OPTIX=OFF -DWITH_CYCLES_HIP=OFF -DWITH_CYCLES_METAL=OFF \
            -DWITH_OPENIMAGEDENOISE=OFF -DWITH_XR_OPENXR=OFF \
            -DWITH_USD=OFF -DWITH_OPENCOLLADA=OFF -DWITH_ALEMBIC=OFF \
            -DWITH_OPENSUBDIV=OFF -DWITH_OPENVDB=OFF \
            -DWITH_POTRACE=OFF -DWITH_JACK=OFF -DWITH_PULSEAUDIO=OFF \
            -DWITH_CODEC_FFMPEG=ON

      - name: Build
        run: |
          cd blender/build
          ninja -j"$(nproc)"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: blender-3.6.23-aarch64
          path: blender/build/bin/blender
          retention-days: 10
